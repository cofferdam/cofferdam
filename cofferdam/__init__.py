"""Cofferdam — a controlled workspace between AI agents and your codebase.

This package is the model-free v0.1 Trust Core. This module exposes only the
version string; behaviour lives in the submodules. No guard, executor,
approval, audit, provider, or network code exists yet.
"""

# Single source of truth for the version. The ".dev0" marks this as an
# unreleased build: v0.1.0 is not shipped until the v0.1 line is hardened.
__version__ = "0.1.0.dev0"
